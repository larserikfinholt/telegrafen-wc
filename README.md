# telegrafen-wc
Komponentbibliotek
`npm run start`

# using the library
`npm install telegrafen-wc`

In main
```
import {start} from "telegrafen-wc";

start()

```




# Build new version
To publish a new version
`npm run build`

`npm run build`

`npm publish`


